define(['./computeFormula', './desc', './model', './view', 'less!./style'], function (computeFormula , desc, model, view) {
  return {
    computeFormula: computeFormula,
    desc: desc,
    Model: model,
    View: view
  };
});
